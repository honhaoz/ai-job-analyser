{
  "name": "apps",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "admin-dashboard",
    "consumer-app"
  ],
  "scripts": {
    "dev": "pnpm --parallel --filter consumer-app --filter admin-dashboard dev",
    "build": "pnpm --filter consumer-app --filter admin-dashboard build",
    "lint": "pnpm --filter consumer-app --filter admin-dashboard lint",
    "format": "pnpm --filter consumer-app --filter admin-dashboard format",
    "format:fix": "pnpm --filter consumer-app --filter admin-dashboard format:fix",
    "typecheck": "pnpm --filter consumer-app --filter admin-dashboard typecheck",
    "test": "pnpm --filter consumer-app --filter admin-dashboard test"
  }
}
